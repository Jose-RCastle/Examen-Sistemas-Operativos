{
  "name": "laboratorio-mmu",
  "version": "1.0.0",
  "private": true,
  "description": "Simulador de seis algoritmos de reemplazo de páginas, sin conexión ni dependencias de ejecución.",
  "scripts": { "test": "node --test tests/engine.test.cjs" },
  "engines": { "node": ">=20" }
}
